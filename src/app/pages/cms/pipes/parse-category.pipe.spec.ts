import { ParseCategoryPipe } from './parse-category.pipe';

describe('ParseCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
